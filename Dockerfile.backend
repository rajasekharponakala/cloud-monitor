FROM python:3.12-slim
WORKDIR /app
COPY monitor/ ./monitor/
COPY config.example.toml ./
ENV PYTHONUNBUFFERED=1
# mount config.toml + db from host/compose; default runs the API
EXPOSE 4000
CMD ["python", "-m", "monitor.server"]
