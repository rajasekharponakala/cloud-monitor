## What / why

## How to test
- [ ] `npm run lint` and `npm run build` in `dashboard/`

## Checklist
- [ ] No tokens or `.db` files committed
- [ ] Docs (`README.md` cost model) updated if provider/cost logic changed
